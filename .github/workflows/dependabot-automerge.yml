name: Dependabot auto-merge

# Auto-merges ONLY Dependabot patch/minor bumps, and only via GitHub's auto-merge —
# so the merge waits for the required CI check (branch protection) to pass first.
# Major bumps are left for manual review.
#
# ⚠ MERGING WAS REMOVED 2026-09-22. This workflow now LABELS a green patch/minor PR
# "ready to merge" instead of merging it. Everything else — the actor gate, the
# metadata fetch, the major-bump handling — is unchanged.
#
# WHY. On 2026-09-21 at 18:27 the repository owner went through the open Dependabot
# PRs and turned auto-merge OFF by hand, three seconds apart, across several repos.
# That sweep is the clearest available statement of intent: PRs and merges are a
# human decision here, not an automated one.
#
# AND THE TIMING MATTERED. Every Dependabot PR was failing CI on the frozen-lockfile
# defect, so auto-merge was enabled but could never fire — the bug was acting as an
# accidental brake. `dependabot-bun-lock.yml` (added in the same commit as this
# change) fixes that defect, which would have RELEASED the brake and started merging
# unattended. The two changes therefore ship together and must never be separated:
# the lockfile fix alone recreates exactly the state that was manually backed out.
on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  automerge:
    if: github.actor == 'dependabot[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: dependabot/fetch-metadata@25dd0e34f4fe68f24cc83900b1fe3fe149efef98 # v3.1.0
        id: meta
        with:
          github-token: ${{ github.token }}
      - name: Label patch/minor as ready to merge
        if: steps.meta.outputs.update-type == 'version-update:semver-patch' || steps.meta.outputs.update-type == 'version-update:semver-minor'
        run: |
          gh pr edit "$PR_URL" --add-label 'ready to merge' \
            || gh pr comment "$PR_URL" --body \
              'Patch/minor Dependabot bump. Queued for a human merge once CI is green — this repo does not auto-merge.'
        env:
          PR_URL: ${{ github.event.pull_request.html_url }}
          GH_TOKEN: ${{ github.token }}
